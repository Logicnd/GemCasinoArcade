# Database Configuration
# Your PostgreSQL database connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# Example: postgresql://myuser:mypassword@db.example.com:5432/gemcasino?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gem_casino"

# NextAuth Configuration
# A secret key for encrypting authentication tokens and sessions
# Generate a strong random secret with: openssl rand -base64 32
# IMPORTANT: Change this in production!
NEXTAUTH_SECRET="your-long-random-secret-change-this-in-production"

# The base URL of your application
# Local development: http://localhost:3000
# Vercel production: https://your-app-name.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Password Security
# Number of bcrypt rounds for password hashing (10-12 recommended for production)
BCRYPT_ROUNDS=12

# Node Environment
# Automatically set by most hosting providers
# Options: development, production, test
NODE_ENV="development"

# ========================================
# OPTIONAL ENVIRONMENT VARIABLES
# (Only needed if using specific services)
# ========================================

# Vercel Postgres (auto-populated by Vercel)
# If you're using Vercel Postgres, these are set automatically
# POSTGRES_URL="postgresql://..."
# POSTGRES_PRISMA_URL="postgresql://..."
# POSTGRES_URL_NON_POOLING="postgresql://..."

# Prisma Accelerate / Data Proxy (optional)
# Only needed if you're using Prisma Accelerate
# PRISMA_DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"

# Storage Database URLs (optional)
# Only if you need separate database connections for specific features
# STORAGE_DATABASE_URL="postgresql://..."
# STORAGE_POSTGRES_URL="postgresql://..."
# STORAGE_PRISMA_DATABASE_URL="postgresql://..."
